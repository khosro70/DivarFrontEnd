export interface CategoryItemPropsInterface {
  category: {
    _id: string;
    icon: string;
    slug: string;
    name: string;
  };
}

export interface SelectOptionPropsInterface {
  control: any;
  errors: any;
}
